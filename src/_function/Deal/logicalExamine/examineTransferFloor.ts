import { IDeal } from '../../../schema/deal'

const examineBuildingType = (row: IDeal) => {
  // if (!row.parsedValue.floor?.value || !row.parsedValue.transferFloor?.value)
  //   return
  // row.logicalExamine.transferFloor = true
  // if (row.parsedValue.transferFloor?.value > row.parsedValue.floor?.value) {
  //   row.logicalExamine.transferFloor = false
  // }
}

export default examineBuildingType
